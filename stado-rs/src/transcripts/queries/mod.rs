//! The entry points a caller has: the inventory of recoverable names, and the
//! two recovery lookups that hand back a value it asked for by name.

pub(super) mod inventory;
pub(super) mod recovery;
