//! What a signed release manifest and the registry's release policy must say
//! before either is accepted, and the shapes their names, paths and digests
//! are held to.

pub(super) mod manifest;
pub(super) mod registry;
pub(super) mod shape;
