//! Configuration, the object plane every other probe reads through, and the
//! capacity that plane admits work against.

pub(in crate::doctor) mod config;
pub(in crate::doctor) mod object_auth;
pub(in crate::doctor) mod quota;
pub(in crate::doctor) mod storage;
