//! Releases: what the channel already holds, what a claim without artifacts
//! means, and whether the exact coordinate this deployment runs is deliverable.

pub(in crate::doctor) mod channel;
pub(in crate::doctor) mod claims;
pub(in crate::doctor) mod integrity;
