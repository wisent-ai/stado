//! The checked-in half of the schema: what a product declares about itself.

pub(in crate::release_pipeline) mod catalog;
pub(in crate::release_pipeline) mod manifest;
pub(in crate::release_pipeline) mod recipe;
