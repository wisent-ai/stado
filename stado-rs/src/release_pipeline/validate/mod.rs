//! Every rule the schema is held to, and the predicates they share.

pub(in crate::release_pipeline) mod manifest;
pub(in crate::release_pipeline) mod predicates;
pub(in crate::release_pipeline) mod roles;
pub(in crate::release_pipeline) mod version;
