//! Turning declared options into a plan: what each one costs and returns,
//! which bounded subset to buy, and what a declaration must say before it is
//! used at all.

pub(super) mod economics;
pub(super) mod select;
pub(super) mod validate;
