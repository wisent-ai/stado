//! Qualification, publication and promotion: the signing identity a release
//! is published under, the immutable coordinate it lands on, and the fleet
//! reconciliation that follows.

pub(in crate::cli::release_submit) mod artifact;
pub(in crate::cli::release_submit) mod promotion;
pub(in crate::cli::release_submit) mod signing;
