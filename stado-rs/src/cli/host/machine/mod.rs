//! The host as a machine: its accounts and runners, the releases it
//! carries, and its configuration.

pub(in crate::cli::host) mod config;
pub(in crate::cli::host) mod releases;
pub(in crate::cli::host) mod users;
