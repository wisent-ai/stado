//! The fleet's own declarations: the canonical registry document, the
//! identities its hosts hold, and the operating-system verbs that run on
//! them.

pub mod host;
pub mod identity;
pub mod registry;
