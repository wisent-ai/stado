//! The report: the two halves joined into one document in [`join`], and that
//! document rendered for a terminal in [`render`].

pub(in crate::cli::seed_freshness) mod join;
pub(in crate::cli::seed_freshness) mod render;
