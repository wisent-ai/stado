//! The programs a macOS host runs: the installer it is sent, the runtime
//! functions that program carries, and the smaller scripts around it.

pub(super) mod install;
pub(super) mod runtime;
pub(super) mod scripts;
